<nav class="navbar navbar-default">
  <div class="container">
    <div class="row">
      <div class="col-12-xs">
        <!-- Brand and toggle get grouped for better mobile display -->
        <div class="navbar-header">
          <button type="button" class="navbar-toggle collapsed"
            data-toggle="collapse" data-target="#bs-example-navbar-collapse-1"
            aria-expanded="false">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <%= link_to root_path, class: 'navbar-brand' do %>
            <div class='composite-logo'>
              <%= image_tag 'billion_logo_small.png', class: 'logo' %><span class='tenant'>Fuse</span>
            </div>
          <% end %>
        </div>

        <!-- Collect the nav links, forms, and other content for toggling -->
        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
          <ul class="nav navbar-nav navbar-right">
            <li><%= link_to 'Our Story', story_path %></li>

            <% if @competition.projects.size > 0 %>
              <li>
                <%= link_to 'Participants', projects_path %>
              </li>
            <% end %>
            <li>
              <%= link_to 'Previous Cycle', 'https://idf.billioneffect.com' %>
            </li>

            <% if @competition.total_raised > 100 %>
              <li class="navbar-total">
                Fuse Beer Fund
                <%= number_to_currency @competition.total_raised %>
              </li>
            <% end %>
          </ul>
        </div><!-- /.navbar-collapse -->
      </div><!-- /.container-fluid -->
    </div>
  </div>
</nav>
